#include <iostream>
#include <cctype> //character library

using namespace std;

int main (){
  char sample = 's';
  
  cout << "before toupper " << sample << endl;
  sample = toupper(sample);    //converts lowercase to uppercase
  cout << "after to upper " << sample << endl;
  
return 0;
}
